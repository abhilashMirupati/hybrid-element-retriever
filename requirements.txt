# Core dependencies (align with pyproject and current tooling)
playwright>=1.45
numpy>=1.24
pydantic==1.10.12

# Optional dependencies (install if available)
# Skip heavy optional deps on Python 3.13 where these pins lack wheels
faiss-cpu>=1.8.0; python_version < "3.13"
onnxruntime>=1.17,<1.23; python_version < "3.13"
py4j==0.10.9.7

# Optional ML dependencies (can work without these)
transformers>=4.38,<4.45
tokenizers>=0.15.2,<0.21
huggingface_hub>=0.20.0

# Type hints
types-requests==2.31.0.4
